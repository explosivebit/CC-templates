# Build & Test Configuration

> Generated by `/setup`. Edit when stack changes.

## Stack

**Language**: {language + version, e.g. "TypeScript 5.4 / Node 20"}
**Package manager**: {npm | pnpm | yarn | cargo | uv | pip | go | other}
**Workspace**: {monorepo (pnpm-workspace / cargo workspace / go work) | single package}
**Test framework**: {vitest | jest | pytest | cargo test | go test | ...}

## Commands

| Action | Command |
|--------|---------|
| Install deps | `{cmd}` |
| Build (all) | `{cmd}` |
| Build (single package) | `{cmd, e.g. pnpm --filter <name> build}` |
| Test (all) | `{cmd}` |
| Test (single) | `{cmd}` |
| Type-check | `{cmd, e.g. tsc --noEmit / mypy / cargo check}` |
| Lint | `{cmd}` |
| Format | `{cmd}` |
| Dev server | `{cmd, if applicable}` |
| Generate code | `{cmd, if applicable, e.g. prisma generate}` |

## Project conventions

> Free-form notes. Read by skills before they suggest commands.

- {e.g. "After editing `packages/*`, must rebuild that package before tests"}
- {e.g. "Tests live in `*.test.ts` next to source, not in `__tests__/`"}
- {e.g. "Pre-commit hook runs lint + typecheck — don't skip with `--no-verify`"}

## CI

- **Pipeline**: {GitHub Actions / GitLab CI / Jenkins / none}
- **Required checks**: {list}
- **How to view failed runs**: `{cmd, e.g. gh run view --log-failed}`
