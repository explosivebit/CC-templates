# Project Paths

> Generated by `/setup`. Edit when structure changes.

## Documentation

| Type | Path |
|------|------|
| RFC / design docs | `{e.g. docs/rfc/, docs/design/, docs/adr/}` |
| ADR (Architecture Decision Records) | `{e.g. docs/adr/}` |
| TODO file(s) | `{e.g. docs/TODO.md, .scratch/TODO.md}` |
| Architecture / overview docs | `{e.g. docs/architecture/}` |
| Known issues | `{e.g. docs/KNOWN-ISSUES.md, none}` |
| Out-of-scope archive | `{e.g. .out-of-scope/, none}` |

## Code

| Layer | Path | Notes |
|-------|------|-------|
| Source | `{e.g. src/}` | |
| Tests | `{e.g. src/**/*.test.ts}` | colocation pattern |
| Examples | `{e.g. examples/}` | |
| Internal packages | `{e.g. packages/, none}` | monorepo only |
| Apps | `{e.g. apps/, none}` | monorepo only |

## Conventions

- **File naming**: {e.g. kebab-case, snake_case}
- **Test file pattern**: {e.g. `*.test.ts`, `*_test.go`, `test_*.py`}
- **Where to put new RFCs**: {path + naming convention}
- **Where to put new ADRs**: {path + naming convention}
